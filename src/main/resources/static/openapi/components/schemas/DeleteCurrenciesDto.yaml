type: object
required:
  - currency_ids
properties:
  currency_ids:
    type: array
    description: The IDs of currencies that should be deleted.
    nullable: true
    minItems: 1
    items:
      type: integer
