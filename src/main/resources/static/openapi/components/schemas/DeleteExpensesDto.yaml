type: object
required:
  - expense_ids
properties:
  expense_ids:
    type: array
    description: The IDs of expenses that should be deleted.
    nullable: true
    minItems: 1
    items:
      type: integer
